using Microsoft.Data.SqlClient;
using ScreenSound;
using ScreenSound.Banco;
using ScreenSound.Modelos;

class Program
{
    static void Main(string[] args)
    {
        //ScreenSoundContext context = new ScreenSoundContext();
        ////ArtistaDal artistaDal = new ArtistaDal(context);
        //MusicasArtistasDal musicasArtistasDal = new MusicasArtistasDal(context);
        //MusicaDal musicasDal = new MusicaDal(context);

        //var sistema = new SistemaScreenSound(artistaDal,musicasArtistasDal,musicasDal);
        //sistema.Iniciar();

    }
}
